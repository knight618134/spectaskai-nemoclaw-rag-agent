[project]
name = "nemoclaw-research"
version = "0.1.0"
description = "AI agent and lightweight RAG research scaffold for O-RAN operations workflows."
requires-python = ">=3.9"
dependencies = []

[project.scripts]
spectaskai = "spectaskai.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
